# Google Maps Places API key, used for US address autocomplete on Register.
# Restrict this key to your domain(s) and enable the "Places API" in Google Cloud Console.
VITE_GOOGLE_MAPS_API_KEY=
VITE_GOOGLE_MAPS_ENABLED=true
